import { Fragment } from 'react';
import styles from './Login.module.css';

const Login = (props) => {
    return (
        <Fragment>
            <div>
                <h2>Login</h2>
            </div>
        </Fragment>
    )
}

export default Login;